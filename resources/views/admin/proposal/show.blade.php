@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row">
            <div class="col-md-8 col-md-offset-2">
                <div class="panel panel-default">
                    <div class="panel-heading">proposal {{ $proposal->id }}</div>
                    <div class="panel-body">

                        <a href="{{ url('admin/proposal/' . $proposal->id . '/edit') }}" class="btn btn-primary btn-xs" title="Edit proposal"><span class="glyphicon glyphicon-pencil" aria-hidden="true"/></a>
                        {!! Form::open([
                            'method'=>'DELETE',
                            'url' => ['admin/proposal', $proposal->id],
                            'style' => 'display:inline'
                        ]) !!}
                            {!! Form::button('<span class="glyphicon glyphicon-trash" aria-hidden="true"/>', array(
                                    'type' => 'submit',
                                    'class' => 'btn btn-danger btn-xs',
                                    'title' => 'Delete proposal',
                                    'onclick'=>'return confirm("Confirm delete?")'
                            ))!!}
                        {!! Form::close() !!}
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table table-borderless">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $proposal->id }}</td>
                                    </tr>
                                    <tr><th> Nim </th><td> {{ $proposal->nim }} </td></tr><tr><th> Nama </th><td> {{ $proposal->nama }} </td></tr><tr><th> Judul Ta </th><td> {{ $proposal->judul_ta }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection