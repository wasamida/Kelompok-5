@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row">
            <div class="col-md-8 col-md-offset-2">
                <div class="panel panel-default">
                    <div class="panel-heading">announcement {{ $announcement->id }}</div>
                    <div class="panel-body">

                        <a href="{{ url('admin/announcement/' . $announcement->id . '/edit') }}" class="btn btn-primary btn-xs" title="Edit announcement"><span class="glyphicon glyphicon-pencil" aria-hidden="true"/></a>
                        {!! Form::open([
                            'method'=>'DELETE',
                            'url' => ['admin/announcement', $announcement->id],
                            'style' => 'display:inline'
                        ]) !!}
                            {!! Form::button('<span class="glyphicon glyphicon-trash" aria-hidden="true"/>', array(
                                    'type' => 'submit',
                                    'class' => 'btn btn-danger btn-xs',
                                    'title' => 'Delete announcement',
                                    'onclick'=>'return confirm("Confirm delete?")'
                            ))!!}
                        {!! Form::close() !!}
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table table-borderless">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $announcement->id }}</td>
                                    </tr>
                                    <tr><th> Title </th><td> {{ $announcement->title }} </td></tr><tr><th> Content </th><td> {{ $announcement->content }} </td></tr><tr><th> User Id </th><td> {{ $announcement->user_id }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection